{
  "name": "omc-visual-server",
  "version": "0.1.0",
  "description": "Backend server for OMC Visual",
  "type": "module",
  "scripts": {
    "dev": "env PATH=\"${HOME}/.cargo/bin:${PATH}\" node --watch src/index.js",
    "start": "env PATH=\"${HOME}/.cargo/bin:${PATH}\" node src/index.js"
  },
  "dependencies": {
    "fastify": "^5.3.0",
    "@fastify/websocket": "^11.0.0",
    "@fastify/static": "^8.1.0",
    "@fastify/cors": "^10.1.0",
    "node-pty": "^1.0.0",
    "chokidar": "^4.0.0",
    "better-sqlite3": "^11.8.0"
  }
}
